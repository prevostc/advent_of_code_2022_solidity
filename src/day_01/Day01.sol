// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.13;

contract Day01 {
    function answer(string memory input) public {
        int256 maxSum = 0;
        int256 maxIdx = -1;
        // split string to iterate

        // parse each line as int
        // sum until we find an empty string or end of array
        // keep index of elf with max sum
    }
}
